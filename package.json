{
  "name": "test-codex-project",
  "version": "0.1.0",
  "private": true,
  "description": "A Tauri desktop demo project that builds into a native executable.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "start": "npm run dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.1"
  }
}
